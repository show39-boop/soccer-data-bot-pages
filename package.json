{
  "name": "soccer-data-bot-pages",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name soccer-data-bot"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
